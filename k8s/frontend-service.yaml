apiVersion: v1
kind: Service
metadata:
  name: pdf-extractor-frontend
spec:
  selector:
    app: pdf-extractor-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30080
  type: NodePort
